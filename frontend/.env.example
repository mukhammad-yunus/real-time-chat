# Server-only: Route Handlers and Server Components use this.
BACKEND_URL=http://localhost:4000

# Browser-visible: Socket.IO uses this in local development.
NEXT_PUBLIC_SOCKET_URL=http://localhost:4000

# Used by sitemap and metadata.
NEXT_PUBLIC_APP_URL=http://localhost:3000
